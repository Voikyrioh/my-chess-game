export type {BaseModal} from './base-modal';
import {promoteModal} from './promote-modal';
import {checkmateModal} from './checkmate-modal';

const modal = { promoteModal, checkmateModal };

export default modal;
